// Lists all documents with name="ALX" in the school collection.
db.school.find({ name: "ALX" }).forEach(printjson);

// Lists all documents with names starting with "ALX" in the school collection.
db.school.find({ name: /^ALX/ }).forEach(printjson);